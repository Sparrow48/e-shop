import axios from "axios";
import { useState } from "react";

const useAuth = (apiConfig) => {
  const [data, setData] = useState("");
  const [error, setError] = useState("");
};

export default useAuth;
